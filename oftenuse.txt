# 第一步：查看状态（可选，确认你改了哪些文件）
git status

# 第二步：暂存更改（将修改的文件加入待提交名单）
# "." 代表当前目录下所有改动。你也可以指定文件名，如 git add _pages/about.md
git add .

# 第三步：提交到本地仓库
# 这里的字符串是你的修改记录，建议写得清晰一些
git commit -m ""

# 第四步：推送到 GitHub
# 因为你配了 SSH，这步会非常快且不需要密码
git push origin main

git pull origin main	在修改前运行一下，确保本地代码和云端是最同步的。